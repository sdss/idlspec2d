File=Table
Product=idlspec2d

Group:
Flavor=ANY
Qualifiers=""

Common:
   Action=setup
      setupOptional("-f ${UPS_PROD_FLAVOR} elodie")
      setupOptional("-f ${UPS_PROD_FLAVOR} spinspect")
      setupOptional("-f ${UPS_PROD_FLAVOR} specflat")
      setupRequired("-f ${UPS_PROD_FLAVOR} idlutils")
      proddir()
      setupenv()
      pathAppend(PATH, ${UPS_PROD_DIR}/bin)
      envPrepend(IDL_PATH, +${UPS_PROD_DIR}/pro)
End:

