File=Table
Product=evilspec
#*************************************************

Group:
Flavor=Linux+2
Qualifiers=""

Common:
   Action=setup
      proddir()
      setupenv()
      envSet(EVIL_STARTUP, ${UPS_PROD_DIR}/etc/evilStartup.tcl)
      pathAppend(PATH, ${UPS_PROD_DIR}/bin)
End:

#*************************************************
